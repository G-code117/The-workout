<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{gif}}}</p>
  <p>{{equipment}}</p>
  <p>{{instructions}}</p>
  <p>best workout for {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="Get That Gain">💪</span>
    <span class="muscle-group">{{exercise}}</span>
  </p>
  <p>
    <button class="btn btn-primary">add this exercise!💪</button>
  </p>
</div>